// ============================================================
// config.js — Configuration côté client
// Plus de token ici ! Le token Discogs est sur le serveur Vercel.
// ============================================================

export const CONFIG = {
  // Endpoint du proxy serverless (voir /api/discogs.js)
  DISCOGS_PROXY: '/api/discogs',
};
